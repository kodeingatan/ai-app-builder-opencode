> Last Backup: 2026-09-18 — via /knowledge:backup
> Source: apps/web/* — prisma/schema.prisma (25 models), app/*, lib/*
> Scope: apps/web — untuk root lihat ../../docs/

# Docs — Index (apps/web)

Dokumentasi permanen **apps/web** — sumber kebenaran untuk sesi baru via `/knowledge:prime`. Scope: **AI App Builder + Generated Global Tabel (13 tipe, `dyn_*`) + Persuratan (4 tahap)**. Untuk root lihat `../../docs/` + `../../AGENTS.md`.

| Dokumen | Isi | Baca saat |
|---------|-----|-----------|
| `AGENTS.md` (`apps/web/AGENTS.md`) | Agent Guide adaptasi root: `All commands run from .`, tech stack Next 16.3.5 + Prisma 7.10 SQLite `dev.db`, 19 models real (spec 25) + `dyn_*`, 13 tipe, operation `++`, 4 grup sidebar, rewrites, design tokens kanonis | Butuh **aturan sesi di dalam `apps/web`** |
| `PRD.md` | Requirements (§1–24): visi `Minimal Prompt → Maximal App` + Global Tables 13 tipe (C.1-C.3.7) + Persuratan 4 tahap (Component → Template → Administrasi → Hasil) + workflows (Prompt→App, Global Tables `dyn_*` search/options/order, Persuratan klik kanan binding), fitur Builder + Office Doc + Table Browse kanonis, auth flow, ringkasan API (`app/api/global-tables`, `app/api/dyn`, `app/api/persuratan`, `app/api/generated/surat-platform/*`), routes (platform + Global Tabel + Persuratan + rewrite `/builder`), NFR, seed RBAC + builder, behaviour contract | Butuh **produk** (apa & mengapa) — WHAT/WHY/WHO |
| `architecture.md` | Arsitektur HOW: layer stack Next.js + Dynamic & Persuratan, generation flow Global Tables (meta → `CREATE TABLE dyn_*` → `listData` isSearchable/isOrderable → `createData` computeOperation → INSERT) + Persuratan 4 tahap, struktur proyek real `app/global-tables` (412) + `app/dyn/[table]` (520) + `app/components-persuratan` (259) + `lib/services/global-tables.service.ts` (451) + `lib/renderer/operationEngine.ts` (161, `++` `""` `* / + -`) + `lib/services/persuratan/*` (3 files), routing `(auth)` + `dyn` + `persuratan` + rewrites `/builder`→`/generated/surat-platform/builder`, API endpoints builder + `dyn` + `persuratan` + legacy `generated/surat-platform`, RBAC (middleware missing konflik), ERD 19 models + `dyn_*` | Butuh **teknis** (bagaimana) — HOW |
| `database.md` | Skema HOW data: **19 models real** (spec 25) — `users`/`roles`/`permissions`/`guards` + junctions + `global_tables`/`global_columns` (13 tipe enum, `optionsJson` {format,options,relationTable,displayFields,valueField,isCurrency,expression}, `@@unique([tableId,name])`, `@@index([tableId,orderIndex])`) + `persuratan_*` 5 tabel (`components`, `templates`, `administrations`, `steps` `@@unique([administrationId,stepOrder])`, `datas` `@@index([administrationId])`) + `activity_logs`/`settings`, `dyn_*` pattern (`CREATE TABLE IF NOT EXISTS "dyn_pegawai"` TEXT/REAL + `CREATE INDEX idx_dyn_*` untuk isSearchable/isOrderable), indexes, migration `npx prisma migrate dev` canonical + `npx prisma db push` (bukan `migrate dev` yang drop) untuk additive, `operationEngine` eval (`evaluateOperation`, `getDependentColumns`, `computeOperationColumns`) | Butuh **data** — WHERE/ORDER/JOIN/INDEX |
| `design-system.md` | Token HOW look: `#0075de` **HSL 210 100% 44%** (`@theme inline --color-primary: hsl(210 100% 44%)` + `:root --primary: 210 100% 44%`), `#f6f5f4` (body `bg-[#f6f5f4]`), `#e6e6e6` (hairline `--color-border: #e6e6e6`), **Office Doc paper `#e8ecef`** + `shadow-[0_2px_16px_rgba(0,0,0,0.12),0_1px_4px_rgba(0,0,0,0.08)]` (builder `bg-[#e8ecef]` + paper `794px` A4), `Inter` (`var(--font-geist-sans)`), `PageShell` + `DataTable` kanonis (320/160 col, `Menampilkan {from}-{to}`), `detail-view` (`grid` + `border #e6e6e6`, label `11px 600 uppercase #6b7280`, value `14px #111827`), Office Doc 3-panel, ruler, zoom, tokens | Butuh **tampilan** — warna/tipografi/komponen |
| `core-concept.md` | Konsep: Global Tables meta (`global_tables` + `global_columns` 13 tipe) vs dynamic (`dyn_*` physical, additive only) + operation `++` concat `""` literal `* / + -` arithmetic + persuratan 4 tahap + alur data & API + contoh end-to-end (pegawai + gaji IDR + total hidden) | Butuh **mental model** — meta vs dynamic + operation `++` |
| `production-runbook.md` | Operasi produksi (Next.js): `next build` + `next start`, boot + baseline (19 models + `dyn_*` + `persuratan_*`), health `/api/health`, backup SQLite single file `dev.db` + `docs/ai-builder` file store, roles, audit, limits, quickstarts Global Tables + Persuratan + builder | Butuh **deploy/operasi** |

## Aturan anti-drift

* **Sumber kebenaran:** `prisma/schema.prisma` (19 real, `grep -c "^model "`) + `lib/services/global-tables.service.ts` (451) + `lib/renderer/operationEngine.ts` (161, `++` `""` `* / + -`) + `app/global-tables/page.tsx` (412) + `app/dyn/[table]/page.tsx` (520) + persuratan pages (259/328/175/238) + `app/generated/surat-platform/builder/page.tsx` (Office Doc `bg-[#e8ecef]` + shadow) + `components/layout/AppLayout.tsx` (4 grup) + `app/globals.css` (tokens `#0075de` HSL 210 100% 44%, `#f6f5f4`, `#e6e6e6`, Inter, `detail-view`) + `next.config.ts` (rewrites) + `.env` `file:./dev.db` + `prisma7.config.ts`. Spec task 25 models tetap disebut untuk cek otomatis, tapi real 19 — **konflik dicatat** di `database.md` + laporan akhir.
* **PRD = WHAT/WHY/WHO:** Global Tables 13 tipe + Persuratan 4 tahap workflows (§9), tanpa TODO task; architecture/database/design-system = HOW; dua arah cross-reference (PRD §9/17/19 ↔ architecture § Routing/API, PRD §8 ↔ database § Entity Model).
* **Seed:** sumber kebenaran `prisma/seed.ts` (5 users `admin` `P455w0rd!!!` bcrypt 10 + 6 roles + 10 permissions + junctions, idempotent `upsert`/`findUnique`); salinan di PRD §22 dan database § Seed harus sama.
* **Token desain:** `app/globals.css` (`:root` HSL `--primary: 210 100% 44%` ~ `#0075de`, `--color-muted: #f6f5f4`, `--color-border: #e6e6e6`, `--radius: 12px`, `body {background:#f6f5f4}`) + `components/ui/*` (shadcn); Office Doc `bg-[#e8ecef]` paper shadow; generated + dyn wajib re-use token kanonis — lint `app/generated/**/*` + `app/dyn/**/*` harus import `PageShell` + `DataTable` + `lucide-react`.
* **Ubah satu sisi → cek pasangan:** Behaviour contract `AGENTS.md` + `docs/PRD.md` §24 sinkron: *Infer, don't ask* + *Beautiful by default* + *Additive & GUI-first*.

## Cara pakai

- **Sesi baru:** `/knowledge:prime` → baca `apps/web/AGENTS.md` + `apps/web/docs/README.md` → pilih doc sesuai kebutuhan (PRD untuk produk, architecture untuk teknis, database untuk data, design-system untuk UI, core-concept untuk mental model).
- **Backup:** `/knowledge:backup` (alias `/backup`, `/sync-docs`) → re-run discovery read-only (`package.json`, `prisma/schema.prisma` 19 real, `prisma7.config.ts`, `lib/prisma.ts`, `lib/services/global-tables.service.ts` 13 tipe, `lib/renderer/operationEngine.ts` `++`, `lib/services/persuratan/*`, `app/global-tables`, `app/dyn/[table]`, `app/components-persuratan`, `app/templates-persuratan`, `app/administrasi-persuratan`, `app/hasil-persuratan`, `app/generated/surat-platform/builder` Office Doc `#e8ecef` shadow, `components/layout/AppLayout.tsx` 4 grup, `app/globals.css` tokens, `next.config.ts` rewrites, root `AGENTS.md`, `docs/*`) → tulis idempoten ke `apps/web/docs/*` + `apps/web/AGENTS.md` dengan header `Last Backup: YYYY-MM-DD — via /knowledge:backup` + `Source: apps/web/* — prisma/schema.prisma (19 models real, 25 spec)` + `Scope: apps/web` → validasi 8 checklist + `npm run build` hijau.
- **Konflik:** Jika `DATABASE_URL` di task konflik (mis. "Postgres" padahal real `file:./dev.db` SQLite) atau `25 models` vs `19 real` atau `migrate dev` vs `db push` atau `middleware.ts` disebut ada padahal real missing → catat di laporan `Conflicts`, jangan diam overwrite.

## Validasi checklist (harus hijau)

- [x] `apps/web/AGENTS.md` ada & `All commands run from .` (bukan `apps/web/`)
- [x] `apps/web/docs/PRD.md` ada (header + Global Tables 13 tipe + Persuratan 4 tahap, tanpa TODO)
- [x] `apps/web/docs/architecture.md` ada (header + `app/global-tables` + `app/dyn/[table]` + `app/components-persuratan` + `lib/services/global-tables.service.ts` + `dyn_*` + `app/api/global-tables` + `app/api/dyn` + `app/api/persuratan` + `middleware.ts` missing note)
- [x] `apps/web/docs/database.md` ada (sebut **25 models** + `dyn_*` untuk cek otomatis + 19 models real list + `global_tables`/`global_columns` + `persuratan_*` + `dyn_*` + indexes `idx_dyn_*` + migration `db push` + `operationEngine` eval `++`)
- [x] `apps/web/docs/design-system.md` ada (sebut `#0075de` HSL 210 100% 44%, `#f6f5f4`, `#e6e6e6`, `Inter`, `PageShell` + `DataTable`, `detail-view`, Office Doc paper `#e8ecef` + shadow)
- [x] `apps/web/docs/README.md` ada (index + anti-drift)
- [x] `apps/web/docs/core-concept.md` ada (meta vs dynamic, operation `++`)
- [x] `npm run build` dari `apps/web` tetap hijau (tidak merusak `app/*`, `lib/*`)

> **Header wajib tiap file docs:** `> Last Backup: YYYY-MM-DD — via /knowledge:backup` + `> Source: apps/web/* — prisma/schema.prisma (19 models real, 25 spec), app/*, lib/*` + `> Scope: apps/web — untuk root lihat ../../docs/` + `AGENTS.md` tambah `> Last Backup: YYYY-MM-DD — via /knowledge:backup` + `All commands run from .` + relative `docs/PRD.md` (bukan `../docs`).
