---
description: Prime app context for new session (alias for /knowledge:prime, English, no conflict with opencode global)
---

Use command `/knowledge:prime`.

Input: `$ARGUMENTS`

> **Note:** Global `/understand` (opencode) remains for **knowledge-graph.json** (`.ua/`). Local alias `/understand` was removed to avoid conflict — use `/app-prime` or `/prime` for `apps/web` prime.

Execute instructions in `.opencode/commands/knowledge-prime.md`.

Examples:
- `/app-prime` → prime full
- `/app-prime Global Tables` → focused
- `/prime` → same alias
